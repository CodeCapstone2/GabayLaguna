Gabay Laguna - Login Credentials

Admin
- Email: admin@gabaylaguna.com
- Password: Password123!
- Login: /login (automatic redirect to /admin-dashboard)

Tour Guide
- Email: guide@gabaylaguna.com
- Password: Password123!
- Login: /login (select Guide)

Tourist
- Email: tourist@gabaylaguna.com
- Password: Password123!
- Login: /login (select Tourist)

Notes
- These credentials are seeded by the backend. Run database setup to ensure they exist.
  From backend folder:
    php artisan migrate --seed
  If PHP is not on PATH, install PHP 8.2+ and Composer, then run:
    composer install
    cp .env.example .env (or copy manually) and configure DB
    php artisan key:generate
    php artisan migrate --seed
- API Base URL (default): http://127.0.0.1:8000
- Login endpoint: POST /api/login with { email, password }
